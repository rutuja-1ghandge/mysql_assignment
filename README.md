# mysql_assignment
This repository contains my solved MySQL assignment using the Classic Models database. It includes queries for SELECT, JOINS, CASE, GROUP BY, views, stored procedures, window functions, triggers, and database constraints—all organized topic-wise.
